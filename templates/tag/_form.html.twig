{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Создать тег') }}</button>
    <a class="btn btn-secondary" href="{{ path('tag_index') }}">Отмена</a>
{{ form_end(form) }}
